version: '3.8'

services:
  wilddict:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wilddict-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
